#ifndef _FACTOR_H_
#define _FACTOR_H_


#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#define _GNU_SOURCE


/* size_t getle(char **lptr, size_t *FILE *stream); */
int factorize(char *buffer);

#endif /* _TOR_H_ */
